class Solution {
    public boolean doesValidArrayExist(int[] derived) {
        int i,count=0;
        for(i=0;i<derived.length;i++)
        {
            count=count^derived[i];
        }
        if(count==0)
        {
            return true;
        }
        else
        {
            return false;
        }
    }
}
